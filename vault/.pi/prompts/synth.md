---
description: Cluster what I've already written on a topic; propose links and a .base
argument-hint: "<topic>"
---
Load `vault-syntax`. Synthesize **$1** across the vault.

`rg` notes (not Bin/, not .obsidian/). Cluster AL / BSc / OCW / CS50 hits. Surface contradictions and orphans. Propose `[[wikilinks]]` — do not apply a bulk rewrite.

End with a proposed `.base` (never a Dataview block) that would keep this query reusable. Show it; only write the `.base` if I say yes. For large sets, fan out with `@academic` / sub-agents per folder, then consolidate.
