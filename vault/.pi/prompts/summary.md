---
description: Compact single-topic Summary.canvas next to the covering notes
argument-hint: "<topic>"
---
Load `canvas-gen` and `vault-syntax`. Build a compact `Summary.canvas` for **$1** next to the notes that cover it (course folder, not vault root).

Few file nodes, short labelled edges, one group. Graph from me, geometry from `layout.py --merge`. Hex colours from the course `background`.
