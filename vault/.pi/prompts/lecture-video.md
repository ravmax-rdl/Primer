---
description: YouTube lecture → timestamped draft for a subject note
argument-hint: "<url>"
---
Load `vault-syntax`. Analyse **$1** with `pi-web-access` YouTube understanding.

Draft a structured lecture outline with timestamps. Under the title, emit a fenced `cardlink` block (auto-card-link is live) with at least `url` and `title`; add `description`/`host` if you have them — not a bare YouTube URL.

Ask which `W##/D##/<Course>.md` to write into before touching the vault. Filename must equal the course name. `/preview` if math. Do not replace an existing full note — append or fill gaps.
