---
description: Step-by-step derivation, hiding the next step until I commit
argument-hint: "<problem>"
---
Work through: $@

Load `vault-syntax`. Present the derivation inside `> [!Equation]` as `$$ ... $$` using `align*` (or `flalign*` if you need left-flush, as in `Markdown Snippets.md`). Reveal ONE step at a time. After each step, pause and ask me (via `ask_user`) what comes next *before* you reveal it. Continue only once I commit an answer. Keep the struggle in the material — do not hand me the full solution up front. If I ask, give a hint, not the step. Chemistry uses mhchem: `$\ce{...}$`. `/preview` before writing the finished block to a note.
