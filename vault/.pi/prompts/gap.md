---
description: Syllabus vs written notes — what was taught but never written up
argument-hint: "<course>"
---
Load `vault-syntax` and `pdf-search`. Gap report for **$1**.

Compare the course folder's lecture PDFs / handouts against existing `W##/D##/$1.md` notes. List topics taught (slides exist) but never written up. Check `Computer Systems.md` vs the `Computer Systems..md` typo — do not create a third filename.

Output a short table (or `- [?]` lines). Offer `/lecture $1` or `/overview $1` next; do not auto-create a note per gap.
