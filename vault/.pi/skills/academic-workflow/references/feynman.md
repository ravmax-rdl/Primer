---
description: I explain a note to you; you find and patch only the gaps
argument-hint: "<note>"
---
Feynman mode on **$1**. Load `vault-syntax`.

Ask me to explain the note in my own words. Listen for gaps, hand-waving, and errors. Ask one probing follow-up at a time via the `ask_user` tool. When you find a genuine gap, patch ONLY that section (edit in place): mark it `- [/]` or `- [?]` on the Understanding map, add a `> [!note]` or `> [!Equation]` only for the missing claim, `/preview` if math, and show me the diff. Do not rewrite what I already understand, and do not turn this into a lecture — the point is to surface what I *can't* explain.
