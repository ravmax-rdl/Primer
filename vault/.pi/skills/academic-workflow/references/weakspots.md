---
description: Rank topics by SM-2 lapses and trace them back to lessons
---
Load `spaced-repetition` and `canvas-gen`.

Walk `Study Notes/Review/**` with `sm2.py peek` or read each card's `lapses` field. Rank high-lapse cards and follow every `source` link to its lesson. Separate misconception, missing knowledge, and under-drilled recall.

Propose a targeted `teach.md` for the worst strand. You may sketch an `overview.md <course> --exam` graph, but do not write the Canvas without approval.
